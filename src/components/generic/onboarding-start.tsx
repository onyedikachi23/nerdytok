/** @format */

import { Text, View } from "react-native";
import { AppText } from "../customized";

export default function OnboardingStart() {
	return (
		<View>
			<AppText></AppText>
		</View>
	);
}
